# THE THREAD — the connectedness campaign

*Design + verdicts doc, 2026-07-12. Method: the Hand playbook (census → cause
modes → single-axis probes → assembly → distribution referee). Owner's laws:
tweens stay lulls / user-steerable / subplot-natured; threads are typed, never
object-only; "none" always legal.*

## Census (40 judged Phase-C stories, 39 low connected/consequences verdicts)

- **Mode 1 (dominant): causally empty connective tissue** — judges praise the
  gate-event chain, fault the pages between as "inserted."
- **Mode 2: semantic scene repeats** (same scene, new wording — invisible to
  clone detectors).
- **Mode 3: consequence fade** (the fact persists, the bearing doesn't).
- **Mode 4: episodic plans** (turns reorderable, worst at low dream).
- **Golden pattern:** every high-connected story runs a concrete anchor chain.

## Probe verdicts so far

| probe | verdict |
|---|---|
| T5 typed thread (object/vow/mark/person/place/question/none) | **WON** — connected 1.00→1.33, judge +1.0; planner types anchors to material (lore→objects, personal→notebook, abstract→sacred name). P(human) 0.25 vs ctl 0.52 = open watch-item. |
| T4 via-lines (causal link rendered on the page) | **FAILED** — judge −3.25, P(human) 0.10. Surfaced causality reads machine-tidy. Redesign queued: **via-plan-only** (the slot stays in the brief so the PLAN must chain; the page never sees the line). |

Control-arm replication: tctl P(human) 0.515 = Phase-C hand-on exactly (p29
stable on fresh seeds).

## T1/T6 — THE UNDERCURRENT (tween connectivity), verified design

**Reframe:** tweens don't lack causality, they lack *membership*. A human lull
belongs to something that recurs (a person, a question, a slow change); an
inserted page belongs to nothing. Subplot presence and braided strands are
measured HUMAN signatures — so the fix aligns connectivity with humanlikeness
instead of against it (via's failure).

**What the verifiers KILLED (design-shaping refutations):**
1. *"Prompt-side data is pink-elephant-safe"* — FALSE. Via's line was
   prompt-side and still collapsed P(human). The damage is what the data does
   to the prose's SHAPE, not word leakage → every strand line must pass the
   P(human) hard-gate; subtler is safer.
2. *Corridor replanning on steer* — doesn't exist. The spine is FIRM; steering
   = drift via propose() producing eventless 'move' pages, and drift resets
   `_tween_k`. Reader-chosen lulls are NOT bridge-mode — a bridge-only
   mechanism misses the live app's actual lulls.
3. *Planner-authored beat ladders* (scheduled undercurrent beats) — a
   pre-authored second chain: chore-risk, chain-risk. Killed.
4. *Rolling strand memory in the cache key* — race-dependent (server builds
   the next plan before the digest thread lands); repeats the hand-ordinal
   bug class. Killed.
5. *New commit-time ledgers* must be re-copied in `peek_after` (copy.copy
   shares them; the prefetch path pollutes) — a precondition, not a nicety.
6. Good news refutation: `observe_canon` ALREADY harvests waypoint figures —
   re-met figures are identity-held by the p26 ledger for free.

**What survived (the lean build, per-component arms):**
- **T6a — thread-on-quiet-pages:** extend the T5 hold to *eventless* pages
  (bridges AND reader-drift moves — the custody rule stated structurally:
  the undercurrent lives wherever there is no plot_event; gates never see it).
  A carried vow turned over in a lull is idling, not work. ~Two lines.
- **T6b — re-meet corridors:** an encounter ledger of figures/waypoints from
  already-committed pages; every ~2nd–3rd corridor prefers re-meeting one over
  fresh scenery (rarity-capped against "of all people, Bram again!").
  Recurrence EMERGES from the walk's own history — no planner authorship, no
  second chain to leak, cannot ghost (canon holds identities), cannot
  constrain steering (built post-hoc from wherever the reader actually went).
  This is the canonical human lull: running into someone you've met.
- **T6c — steering-precedence deferral** (from Design 2, the one piece that
  survived): a fresh steer suppresses any strand touch that page — deferred,
  not dropped. The reader's channel outranks the strand, honestly ragged.
- **Rest-licensing + monotonicity scan:** the strand may rest; a referee scan
  for the dutiful +1-per-page gradient (the second-order machine tell).
- **T6d — planner UNDERCURRENT card: DEFERRED** until T6a/b verdicts. If
  emergent membership moves `connected` without P(human) cost, authored
  strands may never be needed (and tween hold-budget is already crowded:
  steer + waypoint + mood + hand + canon).

**Probe design:** T6a and T6b as separate same-seed arms; instruments = judge
`connected` (primary), subplot-INTEGRATION (contrasting/ironic vs parallel —
presence alone can read grafted), TMP_ORD_007 braid, P(human) hard-gate with
via as the named falsifier, lull-referee question ("rest/texture or plot
chore?"). Steering-stress arm requires gen_path steer plumbing first
(unbudgeted; noted).

## T6 PROBE VERDICTS (2026-07-12, 4 configs/arm, same seeds; controls = tctl/tthr)

| arm | connected | judge | P(human) | verdict |
|---|---|---|---|---|
| tctl (p29 control) | 1.00 | 79.8 | 0.515 | — |
| tthr (T5 thread, event pages) | **1.33** | **80.8** | 0.250 | **standing winner** |
| t6a (thread + quiet pages) | 1.00 | 80.2 | 0.393 | **BENCH** — recovers some P(human) but gives back the connected gain; lull-touches read as idle color, not linkage |
| t6b (re-meet corridors) | 1.00 | 77.8 | 0.303 | **STARVED** — forensic: post-p26/p27 the emergent-figure pool is empty (cast cards absorb the people; ghosts extinguished); the mechanism had nothing to re-meet. Not wrong — obsolete before birth. A rare good failure: a prior win removed this fix's precondition. |

Campaign scoreboard: T5 thread = the one validated connected-mover. Unbuilt
queue: via-plan-only, T2 semantic-repeat gate, T3 bearing line. The thread's
P(human) watch-item (0.25 vs 0.52) must resolve at the assembly referee
(≥20 pairs) before any default flips.

**Known open risks:** T5's own P(human) watch-item is unresolved at n=4 (a
wider thread arm rides along in the next battery); the consistency-vs-
humanlikeness tension (staged, cards) applies to ALL continuity machinery and
only the hard-gate defends against it; server digest lag means any strand
memory is one-page-behind by construction (design reads, never keys, on it).

## INTRO v3 — THE RE-INTRODUCTION LEDGER (2026-07-13, built + probed)

User requirement (read-test #2 follow-up): subsequent pages must not
RE-INTRODUCE already-met concepts. The intro-v2 contract said "once met, the
idea does the work" — a directive without data. v3 gives it the data: the p26
canon-ledger pattern applied to TERMS, behind `DWELL_INTRO_LEDGER`.

- **Harvest** (`observe_canon`): what each committed page INTRODUCED — a bolded
  first-mention, or an acronym meeting the cold gate's exact gloss criteria
  ("met" == what that gate would pass) — into a first-seen, case-deduped set.
  Cast/canon names never enter: the ledgers are disjoint by construction
  (hold-precedence resolved at harvest, not in the prompt).
- **Ride**: `plan.terms_met` at all 3 plan sites — journey-pattern rolling
  memory, EXCLUDED from key(), re-copied in peek_after. Frame line (inside the
  intro contract): "already introduced on earlier pages, carried as known —
  never re-introduced or re-explained: …".
- **Detector INVERSE**: a re-bold or a parenthetical re-expansion of a met term
  → surgical repair note; deliberately conservative (appositives are normal
  prose; fires once/page). The cold-acronym gate now counts met terms as known.

**Probe (3 same-seed pairs, artc, reading config ±ledger):**

| metric | led0 (t6a2+intro2) | led1 (+ledger) |
|---|---|---|
| cross-page re-introductions | 28 | **7 (−75%)** |
| worst per-story bold-hammering | 7×/7×/11× | 4×/3×/**1×** |
| total introduction marks | 66 | 26 |
| r7 judge mean | 91.0 | 90.7 (flat) |
| frame leaks | 0 | 0 |

Read-check (902 led1): "Significant Form" bolded+glossed ONCE at first entry,
then used plainly throughout — including surviving being BOTH the typed thread
and a met term (the thread hold and the ledger hold composed: the story keeps
returning to the concept without ever re-glossing it). No avoidance stilt.
Residual 7: the gate fires once/page and terms ledger only after commit, so
multiple re-bolds on one later page can survive a single repair pass.
Caveat: probe-sig changes planner cache keys, so arms are same-seed/same-spine
but fresh plan draws — mechanical metric is within-story (robust); judge
comparison is distributional. P(human) fingerprint: see humanlikeness.jsonl
(via-lesson guard).
Fingerprint verdict (n=3 pairs, direction not magnitude): P(human) HIGHER with
the ledger on every pair — 0.004→0.481, 0.454→0.980, 0.860→0.996 (arm means
0.44→0.82). No via-collapse; glossary re-marks ARE machine-tidy tells, and
removing them reads human. VERDICT: SHIP-CANDIDATE — include DWELL_INTRO_LEDGER
in the p30 bank-the-win referee bundle.
