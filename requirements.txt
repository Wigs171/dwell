# Full runtime install for running the Dwell web app (reader + server + Learn builder).
#   pip install -r requirements.txt
# Equivalent extras-based install:
#   pip install -e ".[server]"
#
# Python 3.11+ required.

# --- core: reader engine + Learn/ingest pipeline ---
anthropic>=0.49.0
openai>=1.40.0
pydantic>=2.11.0
pydantic-settings>=2.7.0
httpx>=0.27.0
tiktoken>=0.9.0
rich>=13.9.0
pyyaml>=6.0
numpy>=1.26
pypdf>=5.0
pymupdf>=1.24
youtube-transcript-api>=0.6.0

# --- web server ---
fastapi>=0.115
uvicorn[standard]>=0.34
sse-starlette>=2.2
python-multipart>=0.0.18

# --- optional: semantic embeddings (better neighbors + missed-connections) ---
# Pulls PyTorch (large). Without it, Dwell falls back to TF-IDF automatically.
#   pip install -e ".[embeddings]"
# sentence-transformers>=3.0

# --- optional: local audio narration (Kokoro TTS) ---
# Without it, the browser's Web Speech API is used as a fallback.
#   pip install -e ".[tts]"
# kokoro-onnx>=0.4.0
# sounddevice>=0.5.0
